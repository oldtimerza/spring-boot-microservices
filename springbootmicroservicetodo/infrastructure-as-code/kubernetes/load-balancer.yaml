apiVersion: v1
kind: Service
metadata:
  name: external-entry
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      name: http
  selector:
    app: "springmicroservices-todo"
